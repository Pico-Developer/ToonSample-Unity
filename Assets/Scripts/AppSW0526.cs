using Unity.XR.PXR;
using UnityEngine;

public class AppSW0526 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PXR_Manager.Instance.SetSpaceWarp(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}